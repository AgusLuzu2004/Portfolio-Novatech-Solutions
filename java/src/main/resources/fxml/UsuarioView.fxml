<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.geometry.Insets?>
<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>

<BorderPane xmlns:fx="http://javafx.com/fxml"
            fx:controller="com.novatech.controller.UsuarioController">

    <top>
        <VBox spacing="10" style="-fx-padding: 10;">

            <Label text="Gestión de Usuarios"
                   style="-fx-font-size:20px; -fx-font-weight:bold;"/>

            <HBox spacing="10">

                <Label text="Buscar:"/>

                <TextField fx:id="txtBuscar"
                           promptText="Usuario"/>

                <Button text="Buscar"
                        onAction="#buscarUsuario"/>

            </HBox>

        </VBox>
    </top>

    <center>

        <TableView fx:id="tablaUsuarios"
                   onMouseClicked="#seleccionarUsuario">

            <columns>

                <TableColumn fx:id="colId" text="ID"/>

                <TableColumn fx:id="colUsuario" text="Usuario"/>

                <TableColumn fx:id="colNombre" text="Nombre"/>

                <TableColumn fx:id="colRol" text="Rol"/>

                <TableColumn fx:id="colActivo" text="Activo"/>

            </columns>

        </TableView>

    </center>

    <right>

        <VBox spacing="10"
              style="-fx-padding: 10;"
              prefWidth="220">

            <Label text="Detalle del usuario seleccionado"
                   style="-fx-font-weight:bold;"/>

            <Label text="Usuario:"/>
            <TextField fx:id="txtUsuario" editable="false"/>

            <Label text="Nombre:"/>
            <TextField fx:id="txtNombre" editable="false"/>

            <Label text="Rol:"/>
            <ComboBox fx:id="cmbRol" disable="true" prefWidth="200"/>

            <CheckBox fx:id="chkActivo" text="Activo" disable="true"/>

        </VBox>

    </right>

    <bottom>

        <HBox spacing="10"
              alignment="CENTER"
              style="-fx-padding:10;">

            <Button fx:id="btnNuevo"
                    text="Nuevo"
                    onAction="#nuevoUsuario"/>

            <Button fx:id="btnEditar"
                    text="Editar"
                    onAction="#editarUsuario"/>

            <Button fx:id="btnEliminar"
                    text="Eliminar"
                    onAction="#eliminarUsuario"/>

            <Button text="Activar"
                    onAction="#activarUsuario"/>

            <Button text="Desactivar"
                    onAction="#desactivarUsuario"/>

        </HBox>

    </bottom>

</BorderPane>