# Copiá este archivo como "db.properties" (en la misma carpeta) y completá
# tus propios datos. "db.properties" NO se sube al repositorio (ver .gitignore).
#
# Alternativa: en vez de usar este archivo, podés definir las variables de
# entorno DB_URL, DB_USER y DB_PASSWORD, que tienen prioridad sobre este archivo.

db.url=jdbc:mysql://localhost:3306/novatech
db.user=root
db.password=10032004